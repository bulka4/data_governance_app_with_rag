apiVersion: v1
kind: Service
metadata:
  name: mcp
  namespace: semantic-search
spec:
  type: {{ .Values.service.type }}
  selector:
    app: semantic-search-mcp
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP