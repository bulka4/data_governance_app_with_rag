apiVersion: v2
name: data-preparation

description: |
  Metadata extraction Helm chart - extracting metadata from a source database, preparing data lineage data 
  and saving it in another database.

type: application

version: 0.1.0

appVersion: "1.0.0"